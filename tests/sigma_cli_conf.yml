validators:
    - all
    - -tlptag
    - -tlpv1_tag
    - -sigmahq_logsource_known
    - -sigmahq_fieldname_cast
    - -sigmahq_filename_prefix
    - -sigmahq_categorie_eventid
    - -sigmahq_ofselection_condition

exclusions:
    # exclusions can go here by using the GUUID of the rule as the key: folowed by the reason for excluding such as escaped_wildcard
    # escaped_wildcard
    021310d9-30a6-480a-84b7-eaa69aeb92bb: escaped_wildcard
